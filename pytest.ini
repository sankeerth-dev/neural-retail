[pytest]
minversion = 7.0
addopts = -ra -q --tb=short
testpaths = tests
markers =
    integration: marks tests as integration tests (require Docker Compose stack)
    unit: marks tests as unit tests (no external dependencies)
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
